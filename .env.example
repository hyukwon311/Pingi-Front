# 핑이 프론트엔드 환경변수

# API 서버 URL (Docker 빌드 시에는 docker-compose.yaml의 build args가 우선한다)
# 로컬 개발(npm run dev)에서는 Vite 프록시 또는 직접 백엔드 주소를 사용한다.
VITE_API_URL=http://localhost:8100/v1

# WebSocket URL (Socket.io는 HTTP로 연결 후 업그레이드)
VITE_WS_URL=http://localhost:8100
